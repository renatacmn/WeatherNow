<resources>

    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
        <item name="android:windowTranslucentNavigation">true</item>
        <item name="android:windowTranslucentStatus">true</item>
    </style>

    <!-- Text -->
    <style name="TextBodyLight" parent="TextAppearance.AppCompat.Body1">
        <item name="android:textColor">@android:color/white</item>
        <item name="android:fontFamily">sans-serif-light</item>
    </style>

    <style name="TemperatureText" parent="TextBodyLight">
        <item name="android:textSize">@dimen/forecast_temperature_text_size</item>
        <item name="android:fontFamily">sans-serif-thin</item>
    </style>

    <style name="TemperatureUnitText" parent="TextBodyLight">
        <item name="android:textSize">@dimen/forecast_temperature_unit_text_size</item>
    </style>

    <style name="SummaryText" parent="TextBodyLight">
        <item name="android:textSize">@dimen/forecast_summary_text_size</item>
    </style>

    <style name="ErrorText" parent="TextBodyLight">
        <item name="android:textSize">@dimen/forecast_error_text_size</item>
    </style>

</resources>
